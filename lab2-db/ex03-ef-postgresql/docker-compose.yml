services:
  postgres:
    image: postgres:15-alpine
    container_name: ex03_postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: TravelDb
    ports:
      - "5422:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
